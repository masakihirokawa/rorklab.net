---
title: "Rork Max でオフライン対応アプリを作る — AsyncStorage と SQLite 活用"
slug: "rork-offline-support"
category: "rork-dev"
level: "intermediate"
date: "2026-03-13T13:00"
author: "Rork Lab"
description: "Rork Max で作るアプリにオフライン機能を追加する方法。AsyncStorage・expo-sqlite でのローカルデータ保存と同期の実装を解説します。"
tags: ["rork", "offline", "asyncstorage", "sqlite", "tutorial"]
premium: false
---
2014年からモバイルアプリ事業を続けてきた個人開発者として、本題に入ります。

## モバイルアプリとオフライン対応

現代のモバイルアプリでは、**ネットワーク接続がない状態でも動作すること**が求められるケースが増えています。電車の中、飛行機内、電波の弱い場所でも使えるアプリは、ユーザー体験が格段に向上します。

Rork Max アプリは React Native ベースなので、ネイティブのストレージ機能を使ったオフライン対応が可能です。

## AsyncStorage — 軽量なキー・バリューストア

設定値・ユーザープロファイル・小さなデータには `AsyncStorage` が最適です。

```
Rork Max へのプロンプト:
「ユーザーの設定（テーマ・言語設定）を AsyncStorage で保存・読み込みする機能を追加してください。」
```

生成されるコード例:

```javascript
import AsyncStorage from '@react-native-async-storage/async-storage';

// データを保存
export async function saveSettings(settings) {
  try {
    await AsyncStorage.setItem('user_settings', JSON.stringify(settings));
  } catch (error) {
    console.error('Failed to save settings:', error);
  }
}

// データを読み込み
export async function loadSettings() {
  try {
    const stored = await AsyncStorage.getItem('user_settings');
    return stored ? JSON.parse(stored) : null;
  } catch (error) {
    console.error('Failed to load settings:', error);
    return null;
  }
}

// データを削除
export async function clearSettings() {
  await AsyncStorage.removeItem('user_settings');
}
```

## expo-sqlite — 本格的なローカルデータベース

To-Do リスト、メモ、オフラインで使う記事など、複雑なデータには `expo-sqlite` を使います。

```
Rork Max へのプロンプト:
「タスクリストを expo-sqlite でローカルに保存する機能を追加してください。
CRUD 操作（作成・読み取り・更新・削除）に対応させてください。」
```

```javascript
import * as SQLite from 'expo-sqlite';

const db = SQLite.openDatabase('myapp.db');

// テーブルの作成
export function initDatabase() {
  db.transaction((tx) => {
    tx.executeSql(
      `CREATE TABLE IF NOT EXISTS tasks (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        title TEXT NOT NULL,
        completed INTEGER DEFAULT 0,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP
      )`,
      [],
      () => console.log('Table created'),
      (_, error) => console.error('Table creation failed:', error)
    );
  });
}

// タスクの追加
export function addTask(title, callback) {
  db.transaction((tx) => {
    tx.executeSql(
      'INSERT INTO tasks (title) VALUES (?)',
      [title],
      (_, result) => callback(result.insertId),
      (_, error) => console.error('Insert failed:', error)
    );
  });
}

// 全タスクの取得
export function getTasks(callback) {
  db.transaction((tx) => {
    tx.executeSql(
      'SELECT * FROM tasks ORDER BY created_at DESC',
      [],
      (_, result) => callback(result.rows._array),
      (_, error) => console.error('Query failed:', error)
    );
  });
}
```

## ネットワーク状態の監視

オフライン時に適切なUIを表示するには、ネットワーク状態を監視します。

```javascript
import NetInfo from '@react-native-community/netinfo';
import { useState, useEffect } from 'react';

export function useNetworkStatus() {
  const [isOnline, setIsOnline] = useState(true);

  useEffect(() => {
    const unsubscribe = NetInfo.addEventListener((state) => {
      setIsOnline(state.isConnected && state.isInternetReachable);
    });
    return unsubscribe;
  }, []);

  return isOnline;
}

// 使用例
function MyApp() {
  const isOnline = useNetworkStatus();

  return (
    <View>
      {!isOnline && (
        <View style={styles.offlineBanner}>
          <Text>オフラインモードです</Text>
        </View>
      )}
      {/* メインコンテンツ */}
    </View>
  );
}
```

## 全体を振り返って

Rork Max でのオフライン対応には:

- **AsyncStorage**: 設定・セッションなど単純なキー・バリューデータ
- **expo-sqlite**: タスク・メモなど構造化データのローカルDB
- **NetInfo**: ネットワーク状態の監視とUIフィードバック

Rork Max へのプロンプトでこれらを組み込んでもらうと、オフラインでも使えるしっかりしたアプリが完成します。

## 12年の個人開発で見えてきたこと

2014年から個人開発でアプリを公開し続けて、累計5,000万ダウンロードを超えた立場で Rork に向き合うと、ここで扱った話題は「収益化までどれだけ早く到達できるか」に直結します。AdMob 月収100万円超の時期に痛感したのは、リリース後の3ヶ月で改善サイクルを何回回せたかが、その後の伸びを決めるという事実でした。

Dolice Labs を 4 サイト並行で運営しながら、Rork の挙動を見ていると、AI 生成のアプリは「最初に動く」までよりも「直し続けられる構造になっているか」が運用の質を決めると感じます。

### リリース直後にチェックしているもの

- クラッシュレポートの上位エラーを24時間ごとに確認しているか
- AdMob/StoreKit の収益ダッシュボードに異常検知が出ていないか
- ユーザーレビューに技術的な指摘が含まれていないか

## 12年の個人開発から見る Rork の使いどころ

2014年から個人開発でアプリを公開し続けて、累計5,000万ダウンロードを超えた立場で Rork に向き合うと、ここで扱った話題は「収益化までどれだけ早く到達できるか」に直結します。AdMob 月収100万円超の時期に痛感したのは、リリース後の3ヶ月で改善サイクルを何回回せたかが、その後の伸びを決めるという事実でした。

国際芸術賞17冠のアーティスト活動と並行して個人開発を続けてきた立場として、AI 生成のアプリは「最初に動く」までよりも「直し続けられる構造になっているか」が運用の質を決めると感じています。Dolice Labs を 4 サイト並行で運営しながら、Rork の挙動を毎日見続けて分かったことです。

## リリース直後にチェックしているもの

- クラッシュレポートの上位エラーを24時間ごとに確認しているか
- AdMob/StoreKit の収益ダッシュボードに異常検知が出ていないか
- ユーザーレビューに技術的な指摘が含まれていないか

この3点をチェックしておかないと、初動の小さな問題が積み重なって、3ヶ月後の継続率に影響してきます。

## まとめ — 次の改善サイクル

Rork で作ったアプリを伸ばすには、次の順で進めるのが安全です。

1. ストア指標を毎日確認するチームの体制づくり
2. クラッシュレポートの上位 3 件を週次で潰す
3. レビューの技術的指摘を月次でロードマップに反映

これら3段階を継続的に回せるかが、長期的な成長を決めます。
