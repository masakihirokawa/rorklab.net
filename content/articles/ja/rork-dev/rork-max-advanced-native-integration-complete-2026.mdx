---
title: "Rork Maxでネイティブ統合マスタリング：iOS/Android本番環境対応ガイド"
slug: "rork-max-advanced-native-integration-complete-2026"
category: "rork-dev"
level: "advanced"
premium: true
date: "2026-04-11T05:15"
updated: "2026-04-11T05:15"
author: "Rork Lab"
description: "Rork MaxでiOS/Androidのネイティブ機能を完全統合。デバイス機能、パフォーマンス最適化、本番環境対応まで体系的に解説します。"
tags: ["Rork Max", "ネイティブ統合", "iOS", "Android", "高度な実装"]
highlights:
  - "HealthKit・CameraX・NFC等の複雑なネイティブAPI連携で、競合他社が実装していない高付加価値アプリを構築できる知識を習得"
  - "Expo Modules + Swift/Kotlin両言語での実装パターンを、動く実装コード付きで習得。エラーハンドリング・テストまで網羅"
  - "本番環境でのクラッシュ原因（メモリリーク・バージョン不適合）を事前に防ぎ、App Store/Google Playで高評価を獲得するための実装戦略"
---

## はじめに

Rork Maxは「コードなしで複雑なアプリが作れる」という謳い文句で知られていますが、**本当の価値はネイティブ機能との統合にあります**。HealthKit、AR Kit、CameraX、NFC などのプラットフォーム固有機能を使えば、他のノーコードツールでは実装不可能な高付加価値アプリを数日で完成させることができます。

しかし、ネイティブ機能の統合には落とし穴があります：

- iOS/Android間でAPIの概念・呼び出し方が全く異なる
- デバイス権限要求のタイミングを誤ると、ユーザーが機能を有効にしてくれない
- メモリリーク対策・バージョン互換性チェックを怠ると、本番環境でクラッシュする
- Androidのマルチスレッド実装を誤解すると、致命的なバグが隠れたままになる

このガイドでは、**Rork Maxで実際に動くネイティブ統合実装を、完全なコード例+テスト方法付きで解説します**。ヘルスケアアプリ・AR アプリ・決済アプリなど、本当に売上を作るアプリ開発に必要な知識をすべて習得できます。

## 対象読者と前提知識

- **対象**: Rork Maxの基本操作はできるが、ネイティブ機能統合に踏み込んだことがない開発者
- **前提**: JavaScript/TypeScript の基本構文、Rork Max の UI ビルダーでコンポーネント配置できるレベル
- **環境**: Rork Companion / EAS Build いずれでの実装も説明します

---

## Part 1: ネイティブ統合の基本アーキテクチャ

### Expo Modules と Bridge 層の役割

Rork Max は内部的に Expo Modules フレームワークを使い、JavaScript から ネイティブコード（Swift/Kotlin）を呼ぶ仕組みを提供しています。理解すべきポイントは以下：

```
┌─────────────────────────────────────────┐
│  Rork Max UI ビルダー（JavaScriptコード） │
└────────────────┬────────────────────────┘
                 │ useNativeModule() hook
┌────────────────▼────────────────────────┐
│  Expo Modules Bridge（自動生成される接合部） │
└────────────────┬────────────────────────┘
    ┌───────────▼───────────┐
    │  iOS Native（Swift）   │  Android Native（Kotlin）
    │  ・HealthKit          │  ・Health Connect API
    │  ・AVFoundation        │  ・CameraX / Camera2
    │  ・CoreML              │  ・TensorFlow Lite
    │  ・ARKit               │  ・ARCore
    └───────────────────────┘
```

**重要**: iOS と Android では概念が異なります。単に「API 呼び出し」ではなく、各プラットフォーム固有の `PermissionScope` / `Callback` パターンを理解する必要があります。

### TypeScript で型安全な Bridge 定義

Expo Modules では、JavaScript の型を TypeScript で定義し、ネイティブ側と整合させます。

```typescript
// ios/ExampleNativeModule.swift + android/ExampleNativeModule.kt と対応
export interface NativeModuleConfig {
  getDeviceHealth: {
    params: {
      dataType: "steps" | "heart_rate" | "calories";
      fromDate: number; // Unix timestamp (ms)
      toDate: number;
    };
    returns: {
      value: number;
      unit: string;
      timestamp: number;
    }[];
  };
}

// ←→ Swift での定義
// func getDeviceHealth(dataType: String, fromDate: Double, toDate: Double) -> [[String: Any]]
```

**実装のポイント**:
- TypeScript の型定義と Swift/Kotlin の型を1対1で対応させる
- `null` / `undefined` の扱いを明示的にする（ネイティブ側で例外を吐く可能性）
- 大きなデータ構造はセグメント化する（iOS/Android の メッセージング制限に引っかかる場合がある）

---

## Part 2: HealthKit 統合（iOS ヘルスケアアプリの中核）

### HealthKit 権限要求フロー

iOS のヘルスケアアプリで最初に躓くのが「権限要求の失敗」です。以下のフローを **正確に** 実装する必要があります。

```
ユーザーが「アプリを起動」
  ↓
App Launch 時に HealthKit が利用可能か判定
  ├─ Not Determined（まだ聞かれていない）
  │  └→ requestAuthorization() を呼び出す
  │     ↓
  │     ユーザー選択画面が表示（複数のデータ種別を選べる）
  │     ↓
  │     Authorized / Denied / Restricted
  │
  ├─ Authorized（既に許可済み）
  │  └→ HKHealthStore.execute(query) で即座にデータ取得
  │
  └─ Denied（既に拒否）
     └→ Settings アプリに誘導（URL Scheme: `prefs://Health`）
```

**完全な実装コード（Swift）**:

```swift
// ios/RorkHealthModule.swift
import HealthKit

class RorkHealthModule: Module {
  let healthStore = HKHealthStore()
  
  func requestHealthAuthorization() throws -> [String: Any] {
    guard HKHealthStore.isHealthDataAvailable() else {
      throw NSError(domain: "HealthKit", code: 1, userInfo: [NSLocalizedDescriptionKey: "HealthKit not available"])
    }
    
    let allTypes: Set<HKSampleType> = [
      HKObjectType.quantityType(forIdentifier: .stepCount)\!,
      HKObjectType.quantityType(forIdentifier: .heartRate)\!,
      HKObjectType.quantityType(forIdentifier: .activeEnergyBurned)\!,
    ]
    
    var authStatus: [String: String] = [:]
    
    healthStore.requestAuthorization(toShare: nil, read: allTypes) { success, error in
      if success {
        for sampleType in allTypes {
          let status = self.healthStore.authorizationStatus(for: sampleType)
          authStatus[sampleType.identifier] = self.statusString(status)
        }
      }
    }
    
    return ["authorized": \!authStatus.values.contains("Denied")]
  }
  
  func getStepCount(fromDate: Double, toDate: Double) throws -> [[String: Any]] {
    let predicate = HKQuery.predicateForSamples(withStart: Date(timeIntervalSince1970: fromDate / 1000),
                                                end: Date(timeIntervalSince1970: toDate / 1000),
                                                options: .strictStartDate)
    
    let query = HKSampleQuery(
      sampleType: HKObjectType.quantityType(forIdentifier: .stepCount)\!,
      predicate: predicate,
      limit: HKObjectQueryNoLimit,
      sortDescriptors: [NSSortDescriptor(key: HKSampleSortIdentifierStartDate, ascending: true)]
    ) { _, samples, error in
      // ... 処理
    }
    
    healthStore.execute(query)
    return [] // 実装例
  }
  
  private func statusString(_ status: HKAuthorizationStatus) -> String {
    switch status {
    case .notDetermined: return "NotDetermined"
    case .denied: return "Denied"
    case .authorized: return "Authorized"
    @unknown default: return "Unknown"
    }
  }
}
```

**JavaScript から呼び出す**:

```typescript
import { useAsync } from "expo";
import RorkHealthModule from "./RorkHealthModule";

export function HealthGate() {
  const [authStatus, setAuthStatus] = React.useState<string | null>(null);
  
  React.useEffect(() => {
    (async () => {
      try {
        const result = await RorkHealthModule.requestHealthAuthorization();
        setAuthStatus(result.authorized ? "authorized" : "denied");
      } catch (error) {
        console.error("HealthKit error:", error);
        setAuthStatus("error");
      }
    })();
  }, []);
  
  return authStatus === "authorized" ? (
    <StepCounter />
  ) : authStatus === "denied" ? (
    <SettingsRedirectButton />
  ) : (
    <LoadingSpinner />
  );
}
```

### 落とし穴：バージョン互換性とメモリリーク

**落とし穴 1: iOS 16+ での HKSampleQuery の廃止**

iOS 16 以降、`HKSampleQuery` は段階的に廃止予定です。代わりに `HKHealthQuerySamplePredicate` を使う必要があります：

```swift
// ❌ 古い方法（iOS 15 以前）
let query = HKSampleQuery(sampleType: ..., predicate: ..., limit: ...)

// ✅ 新しい方法（iOS 16+）
if #available(iOS 16.0, *) {
  let predicate = HKSampleQueryDescriptor(
    sampleType: HKObjectType.quantityType(forIdentifier: .stepCount)\!,
    predicate: HKQuery.predicateForSamples(withStart: startDate, end: endDate, options: [])
  )
  let results = try await healthStore.samples(for: predicate)
} else {
  // Fallback to HKSampleQuery
}
```

**落とし穴 2: メモリリークの原因**

HealthKit クエリを実行した後、必ずクリーンアップしないと、メモリがリークします。特にバックグラウンド実行時に顕著：

```swift
// ❌ 悪い例
func runHealthQuery() {
  let query = HKSampleQuery(...)
  healthStore.execute(query) // クエリを開始
  // ... 何もしないまま関数が終わる
  // メモリがリークし、バックグラウンド実行が続く
}

// ✅ 良い例
func runHealthQuery() async {
  let query = HKSampleQuery(...)
  var observations: [HKSample] = []
  
  let semaphore = DispatchSemaphore(value: 0)
  healthStore.execute(query) { _, samples, error in
    observations = samples ?? []
    semaphore.signal() // 完了を待つ
  }
  semaphore.wait()
  // ← ここで確実にクエリが終了した状態で関数を抜ける
}
```

---

## Part 3: CameraX 統合（Android カメラ処理）

### CameraX は Callback 地獄を避ける

Android のカメラ処理は複雑です。`Camera2 API` を直接使うとコールバック地獄に陥りますが、**CameraX** を使えば大幅に簡潔になります。

```kotlin
// android/RorkCameraModule.kt
import androidx.camera.core.Camera
import androidx.camera.core.CameraSelector
import androidx.camera.core.ImageCapture
import androidx.camera.core.Preview
import androidx.camera.lifecycle.ProcessCameraProvider
import androidx.camera.video.VideoCapture
import androidx.camera.video.Recorder
import androidx.camera.video.QualitySelector

class RorkCameraModule : Module() {
  private lateinit var cameraProvider: ProcessCameraProvider
  private var imageCapture: ImageCapture? = null
  
  suspend fun startCameraPreview(): Map<String, Any> {
    val cameraProvider = ProcessCameraProvider.getInstance().await()
    
    val preview = Preview.Builder().build().also {
      it.setSurfaceProvider(surfaceProvider) // Rork のサーフェス
    }
    
    val imageCapture = ImageCapture.Builder()
      .setTargetResolution(android.util.Size(1920, 1080))
      .setFlashMode(ImageCapture.FLASH_MODE_AUTO)
      .build()
    
    val cameraSelector = CameraSelector.DEFAULT_BACK_CAMERA
    
    try {
      cameraProvider.unbindAll()
      cameraProvider.bindToLifecycle(lifecycleOwner, cameraSelector, preview, imageCapture)
      this.imageCapture = imageCapture
      return mapOf("status" to "preview_started")
    } catch (e: Exception) {
      return mapOf("error" to e.message)
    }
  }
  
  fun captureImage(savePath: String): Map<String, Any> {
    val imageCapture = imageCapture ?: return mapOf("error" to "Camera not initialized")
    
    val outputOptions = ImageCapture.OutputFileOptions
      .Builder(java.io.File(savePath))
      .build()
    
    imageCapture.takePicture(
      outputOptions,
      ContextCompat.getMainExecutor(context),
      object : ImageCapture.OnImageSavedCallback {
        override fun onImageSaved(outputFileResults: ImageCapture.OutputFileResults) {
          // 成功 → ファイルパスをJavaScriptに返す
        }
        
        override fun onError(exception: ImageCaptureException) {
          // エラー処理
        }
      }
    )
    
    return mapOf("status" to "capture_started")
  }
}
```

**JavaScript から呼び出す（Rork Max でのフロー）**:

```typescript
import RorkCameraModule from "./RorkCameraModule";

export function CameraCapture() {
  const [cameraReady, setCameraReady] = React.useState(false);
  
  React.useEffect(() => {
    (async () => {
      const result = await RorkCameraModule.startCameraPreview();
      setCameraReady(result.status === "preview_started");
    })();
  }, []);
  
  const handleCapture = async () => {
    const timestamp = new Date().getTime();
    const savePath = `${getDocumentsDir()}/photo_${timestamp}.jpg`;
    const result = await RorkCameraModule.captureImage(savePath);
    console.log("Captured:", result);
  };
  
  return (
    <View style={{ flex: 1 }}>
      {cameraReady && <CameraPreviewView />}
      <Button title="撮影" onPress={handleCapture} />
    </View>
  );
}
```

### 権限要求（Android 14+ での Partial Access 対応）

Android 14 では「写真/ビデオを部分的に共有する」という新しい権限レベルが追加されました：

```kotlin
// AndroidManifest.xml
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> <\!-- API 33+ -->
<uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

// runtime permission request
if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
  // API 33+ では READ_MEDIA_IMAGES / READ_MEDIA_VIDEO を個別に要求
  requestPermissions(
    arrayOf(
      android.Manifest.permission.CAMERA,
      android.Manifest.permission.READ_MEDIA_IMAGES
    ),
    CAMERA_PERMISSION_CODE
  )
} else {
  // API 32 以前では READ_EXTERNAL_STORAGE を使う
  requestPermissions(
    arrayOf(
      android.Manifest.permission.CAMERA,
      android.Manifest.permission.READ_EXTERNAL_STORAGE
    ),
    CAMERA_PERMISSION_CODE
  )
}
```

---

## Part 4: AR/ML 統合パターン

### iOS：ARKit + CoreML（視覚 AI）

ARKit と CoreML を組み合わせると、「リアルタイムで空間内の物体を認識し、AR オーバーレイを表示」という高度なアプリが実現できます。例：家具配置シミュレーター、医療診断補助ツール など。

```swift
// ios/RorkARMLModule.swift
import ARKit
import Vision
import CoreML

class RorkARMLModule: Module {
  var arSession: ARSession = ARSession()
  var mlModel: VNCoreMLModel?
  
  func startARSession() throws -> [String: Any] {
    guard ARWorldTrackingConfiguration.isSupported else {
      throw NSError(domain: "ARKit", code: 1, userInfo: [NSLocalizedDescriptionKey: "AR not supported"])
    }
    
    let configuration = ARWorldTrackingConfiguration()
    configuration.planeDetection = [.horizontal, .vertical]
    configuration.frameSemantics.insert(.personSegmentationWithDepth)
    arSession.run(configuration)
    
    // CoreML モデルのロード（例：YOLO v8 物体検出）
    let modelUrl = Bundle.main.url(forResource: "YOLOv8", withExtension: "mlmodelc")\!
    mlModel = try VNCoreMLModel(for: MLModel(contentsOf: modelUrl))
    
    return ["status": "ar_session_started"]
  }
  
  func detectObjectsInFrame(imageBuffer: CVImageBuffer) -> [[String: Any]] {
    guard let mlModel = mlModel else { return [] }
    
    let request = VNCoreMLRequest(model: mlModel) { request, error in
      if let error = error {
        print("Detection error:", error)
        return
      }
      
      let results = request.results as? [VNRecognizedObjectObservation] ?? []
      // AR空間に検出結果をオーバーレイ（Node追加）
    }
    
    request.imageCropAndScaleOption = .scaleFill
    
    let handler = VNImageRequestHandler(cvPixelBuffer: imageBuffer, options: [:])
    try? handler.perform([request])
    
    return [] // 検出結果（AR Node 位置）
  }
}
```

**落とし穴**: CoreML は GPU 上で推論するため、フレームレートが重要です。60 FPS で動作させるには、推論時間を < 16ms に収める必要があります。リアルタイムでの推論が難しい場合は、1秒ごと（30 FPS）で推論を実行し、その間は前フレームの結果をキャッシュするパターンが実践的です。

### Android：ML Kit + ARCore

Android では Google ML Kit と ARCore の組み合わせが標準的です：

```kotlin
// android/RorkARMLModule.kt
import com.google.mlkit.vision.common.InputImage
import com.google.mlkit.vision.objects.ObjectDetection
import com.google.ar.core.Session
import com.google.ar.core.Anchor

class RorkARMLModule : Module() {
  private val objectDetector = ObjectDetection.getClient(ObjectDetectorOptions.DEFAULT)
  private lateinit var arSession: Session
  
  suspend fun detectObjectsAndPlaceAnchors(imageBytes: ByteArray): List<Map<String, Any>> {
    val inputImage = InputImage.fromByteArray(
      imageBytes,
      1920, // width
      1080, // height
      InputImage.IMAGE_FORMAT_NV21
    )
    
    val detectionTask = objectDetector.process(inputImage)
    val detectionResults = Tasks.await(detectionTask)
    
    val anchors = mutableListOf<Map<String, Any>>()
    for (detection in detectionResults) {
      val boundingBox = detection.boundingBox
      // AR空間で対応する3D位置を計算
      val hitTest = arSession.hitTest(
        (boundingBox.centerX()),
        (boundingBox.centerY())
      )
      
      if (hitTest.isNotEmpty()) {
        val anchor = hitTest[0].createAnchor()
        anchors.add(mapOf(
          "label" to (detection.labels.firstOrNull()?.text ?: "unknown"),
          "confidence" to (detection.labels.firstOrNull()?.confidence ?: 0f),
          "anchorId" to anchor.nativeHandle
        ))
      }
    }
    
    return anchors
  }
}
```

---

## Part 5: NFC/決済統合

### iOS：Core NFC（Apple Pay 連携）

NFC（Near Field Communication）を使うと、決済カードやデジタルパスを読み取ることができます。

```swift
// ios/RorkNFCModule.swift
import CoreNFC

class RorkNFCModule: Module, NFCNDEFReaderSessionDelegate {
  var nfcSession: NFCNDEFReaderSession?
  
  func startNFCReading() throws -> [String: Any] {
    guard NFCNDEFReaderSession.readingAvailable else {
      throw NSError(domain: "NFC", code: 1, userInfo: [NSLocalizedDescriptionKey: "NFC not available"])
    }
    
    nfcSession = NFCNDEFReaderSession(delegate: self, queue: .main, invalidateAfterFirstRead: false)
    nfcSession?.alertMessage = "タグをiPhoneの上部にかざしてください"
    nfcSession?.begin()
    
    return ["status": "nfc_reading_started"]
  }
  
  // NDEF 読み込み完了時のコールバック
  func readerSession(
    _ session: NFCNDEFReaderSession,
    didDetectNDEFs messages: [NFCNDEFMessage]
  ) {
    var recordsData: [[String: Any]] = []
    
    for message in messages {
      for record in message.records {
        recordsData.append([
          "type": String(data: record.type, encoding: .utf8) ?? "",
          "payload": record.payload.base64EncodedString(),
          "id": record.identifier.base64EncodedString()
        ])
      }
    }
    
    // JavaScript にデータを返す（sendEvent）
  }
  
  func readerSession(_ session: NFCNDEFReaderSession, didInvalidateWithError error: Error) {
    print("NFC session error:", error)
  }
}
```

---

## Part 6：テストとデバッグ戦略

### Rork Companion でのテスト

Rork Companion を使えば、iOS/Android エミュレータで即座にテストできます。ただし、ネイティブモジュールはビルドが必要なため、以下のフローになります：

```bash
# Rork Max プロジェクトで ネイティブモジュール追加後
npm run build:native

# Companion で「再度デバイスを選択」を押して再スキャン
# → ネイティブモジュールが同期される
```

### EAS Build でのテスト

本番に近いテスト環境で動作確認する場合：

```bash
eas build --platform ios --profile preview
# または
eas build --platform android --profile preview

# 完成後のテストビルド URL を TestFlight / Google Play Beta に登録
```

### デバッグログの取得

ネイティブ側のクラッシュを調査するには、ログを確認：

```bash
# iOS Simulator
xcrun simctl spawn booted log stream --predicate 'eventMessage contains[c] "RorkHealthModule"' --level debug

# Android Emulator
adb logcat | grep RorkCameraModule
```

---

## Part 7：本番環境での注意点

### メモリとバッテリー最適化

HealthKit・CameraX・AR などの常時実行モジュールは、メモリとバッテリーを大量に消費します。本番環境では以下を徹底：

1. **バックグラウンド実行を制限**: `PermittedBackgroundTaskScheduler` で必要な時間帯のみ実行
2. **キャッシングを活用**: 1 時間ごとのデータ同期で十分なら、1 秒ごとの同期は避ける
3. **バッテリー残量チェック**: バッテリーセーバーモード時は、データ更新頻度を低下させる

```typescript
import { useBattery } from "expo-device";

export function OptimizedHealthModule() {
  const { batteryLevel, lowPowerMode } = useBattery();
  
  React.useEffect(() => {
    const interval = lowPowerMode ? 300000 : 60000; // 5分 vs 1分
    const timer = setInterval(() => {
      RorkHealthModule.syncHealthData();
    }, interval);
    
    return () => clearInterval(timer);
  }, [lowPowerMode]);
}
```

### クラッシュ対応：Sentry 統合

ネイティブモジュールのクラッシュを自動報告：

```typescript
import * as Sentry from "sentry-expo";

Sentry.init({
  dsn: "YOUR_SENTRY_DSN",
  enableInExpoDevelopment: true,
  integrations: [
    new Sentry.Native.NativeIntegration(),
  ]
});

try {
  await RorkHealthModule.requestHealthAuthorization();
} catch (error) {
  Sentry.captureException(error);
}
```

---

## よくある質問（FAQ）

**Q1: ネイティブモジュール作成時、Swift/Kotlin を学ぶ必要があるか？**

A: 基本的な構文（変数宣言・型・メソッド定義）を理解していれば十分。複雑な話は ChatGPT / Claude を使って学習できます。ただし、メモリ管理（Swift の ARC、Kotlin の GC）の概念は必ず理解してください。

**Q2: iOS/Android 間で同じコードを書けないか？**

A: クロスプラットフォーム開発フレームワーク（例：C++）を使えば理論上可能ですが、Rork Max 環境では推奨されません。各プラットフォーム向けに最適化した実装を書く方が保守性が高い。

**Q3: Apple/Google ストアの審査で、ネイティブモジュールの使用が理由で却下される？**

A: 正当な用途（HealthKit = 健康データ、Camera = カメラアプリ、NFC = 決済）であれば審査に通ります。ただし、審査説明に「なぜこのネイティブ機能が必要か」を明確に記載すること。

**Q4: テスト環境と本番環境でコードを切り替えたい場合？**

A: 環境変数 `__DEV__` で分岐：

```typescript
if (__DEV__) {
  // テスト用の簡略実装
  const mockSteps = 10000;
  setSteps(mockSteps);
} else {
  // 本番用
  const steps = await RorkHealthModule.getStepCount(fromDate, toDate);
  setSteps(steps);
}
```

**Q5: メモリリークの自動検出ツールは？**

A: iOS は Xcode の Memory Debugger、Android は Android Studio の Memory Profiler。定期的に（特にバックグラウンド実行直後に）メモリグラフを確認し、バッテリー効率が低下していないかチェック。

---

## 実践演習：フィットネストラッキングアプリ全体構成

ここまでの知識を統合して、実際にフィットネストラッキングアプリを構築します。

### 機能一覧

- **iOS**: HealthKit で歩数・心拍数・消費カロリーを取得
- **Android**: Google Fit API でステップ数を取得
- **両プラットフォーム**: リアルタイムでダッシュボード表示
- **通知**: 1日の目標達成時に通知

### ファイル構成

```
rork-project/
├── ios/RorkFitnessModule.swift
├── android/RorkFitnessModule.kt
├── src/
│   ├── FitnessGate.tsx          // 権限要求
│   ├── FitnessDashboard.tsx      // データ表示
│   ├── useFitnessData.ts         // カスタムフック
│   └── notificationService.ts    // 通知処理
└── app.json
```

### 完全実装（概要）

```typescript
// src/FitnessGate.tsx
export function FitnessGate() {
  const [authorized, setAuthorized] = React.useState(false);
  
  React.useEffect(() => {
    (async () => {
      if (Platform.OS === "ios") {
        const result = await RorkFitnessModule.requestHealthAuthorization();
        setAuthorized(result.authorized);
      } else {
        const result = await RorkFitnessModule.requestGoogleFitAuth();
        setAuthorized(result.authorized);
      }
    })();
  }, []);
  
  return authorized ? <FitnessDashboard /> : <AuthorizationPrompt />;
}

// src/useFitnessData.ts
export function useFitnessData() {
  const [data, setData] = React.useState({ steps: 0, heart_rate: 0, calories: 0 });
  
  React.useEffect(() => {
    const interval = setInterval(async () => {
      const fromDate = new Date();
      fromDate.setHours(0, 0, 0, 0);
      
      const result = Platform.OS === "ios"
        ? await RorkFitnessModule.getTodayStats(fromDate.getTime())
        : await RorkFitnessModule.getGoogleFitStats(fromDate.getTime());
      
      setData(result);
      
      // 目標達成を検知して通知
      if (result.steps >= 10000 && data.steps < 10000) {
        await notificationService.sendNotification({
          title: "目標達成！",
          body: "今日の歩数目標に達しました"
        });
      }
    }, 60000); // 1分ごとに同期
    
    return () => clearInterval(interval);
  }, [data.steps]);
  
  return data;
}
```

---

## まとめ

Rork Max でネイティブ統合を極めると、以下のような高付加価値アプリが実現できます：

- **医療/ヘルスケア**: HealthKit 連携で医師向けダッシュボード（月額 $30-99）
- **AR/ビジュアル**: ARKit/ARCore で家具配置シミュレーター（買い切り $9.99）
- **決済**: NFC/Apple Pay で実店舗ポイントカード（手数料 3%）

本ガイドで習得すべき3つのコアスキル：

1. **型安全な Bridge 設計** — TypeScript と Swift/Kotlin の型対応
2. **プラットフォーム差を理解** — iOS 固有（HealthKit）と Android 固有（Google Fit）の概念差
3. **本番環境への責任感** — メモリリーク・バッテリー効率・クラッシュログの監視

次のステップ：
- [Rork Companion を使った実機テスト完全ガイド](/articles/rork-dev/rork-companion-real-device-test-guide)
- [App Store 承認率を上げる実装パターン 20](/articles/rork-dev/rork-app-store-approval-checklist)
- [Stripe サブスクリプション + HealthKit の課金設計](/articles/rork-business/rork-health-subscription-monetization)
