---
title: "Rork Maxのアプリで、アイコンを気持ちよく動かすsymbolEffect実装メモ"
slug: "rork-max-sf-symbols-animation-symboleffect-implementation-memo"
category: "rork-dev"
level: "intermediate"
premium: true
date: "2026-07-12T14:30"
noindex: true
updated: "2026-07-12T14:30"
author: "Rork Lab"
description: "Rork Maxが生成したSwiftアプリで、SF Symbolsをbounce・pulse・variableColor・contentTransitionで動かす実装を、動くコードとOSバージョンの出し分け、そして動かしすぎて後悔した落とし穴つきで整理します。"
tags: ["Rork Max", "SF Symbols", "SwiftUI", "symbolEffect", "アニメーション"]
highlights:
  - "お気に入りボタンや読み込み中のアイコンを、数行のsymbolEffectで自然に動かせる状態にできる"
  - "iOS 17と18で使えるエフェクトの違いを1表で把握し、availabilityで安全に出し分けられる"
  - "variableColorで進捗を、contentTransitionで状態変化を表現する、実務で使い分けるための判断軸が手に入る"
---

個人開発で運営している癒し系アプリで、お気に入りのハートを押したときの反応がずっと物足りませんでした。タップすると色が変わるだけ。動きがないぶん、押した実感が薄いのです。指は反応を期待しているのに、画面は静かなまま。この小さなズレが、体験全体の温度を少しだけ下げていました。

Rork Max が出力するのは本物の Swift なので、この手のUIの手触りは iOS 標準の仕組みに素直に乗せられます。SF Symbols を使っているアイコンなら、`symbolEffect` を数行足すだけで、押した瞬間に跳ねたり、読み込み中に脈打ったりするようになります。ライブラリの追加もアセットの差し替えも要りません。

お気に入りボタンとダウンロード中インジケータという二つの題材で、`symbolEffect` の使いどころを手を動かしながら並べていきます。私自身が動かしすぎて逆に見づらくなった失敗も、正直に書きます。

## まず一番小さい一歩：ワンショットのbounce

最初に試したのは、お気に入りボタンを押した瞬間だけ跳ねさせる動きでした。ここで大事なのは、常時アニメーションではなく「押した回数に反応させる」という考え方です。SwiftUI では、値が変わったときにエフェクトを一度だけ再生する `value:` 付きのオーバーロードを使います。

```swift
import SwiftUI

struct FavoriteButton: View {
    @State private var isFavorite = false
    // タップ回数を数え、bounceの再生トリガーにする
    @State private var tapCount = 0

    var body: some View {
        Button {
            isFavorite.toggle()
            tapCount += 1
        } label: {
            Image(systemName: isFavorite ? "heart.fill" : "heart")
                .font(.system(size: 32))
                .foregroundStyle(isFavorite ? .pink : .secondary)
                // tapCountが変わるたびに一度だけ跳ねる
                .symbolEffect(.bounce, value: tapCount)
        }
    }
}
```

`value:` に渡しているのが `isFavorite`（Bool）ではなく `tapCount`（Int）である点が要点です。Bool を渡すと、同じ状態へ戻したときにエフェクトが再生されないことがあります。カウンタを別に持たせておくと、「押すたびに必ず跳ねる」が確実になります。私は最初 Bool を直接渡してしまい、二回に一回しか跳ねない挙動に数十分悩みました。

体言止めで言えば、ワンショット系エフェクトの基本は「回数で駆動する」。この一点を押さえておくと、後の応用がぶれません。

## 常時動かす：読み込み中のpulseとvariableColor

次はダウンロード中のインジケータです。処理が続いている間ずっと動いてほしいので、こちらは繰り返し再生になります。`.pulse` は明滅、`.variableColor` はレイヤーごとに順番に光らせるエフェクトで、電波やダウンロードのような「段階的に進む」表現に向いています。

```swift
struct DownloadingIcon: View {
    let isDownloading: Bool

    var body: some View {
        Image(systemName: "arrow.down.circle")
            .font(.system(size: 28))
            .symbolEffect(
                .variableColor.iterative.reversing,
                isActive: isDownloading
            )
    }
}
```

繰り返し系では `value:` ではなく `isActive:` を使います。`isActive` が `true` の間だけアニメーションが走り、`false` にすると自然に止まります。状態変数と直結できるので、ダウンロードの開始・完了に合わせて動きを切り替える処理がとても素直に書けます。

`.iterative` は一つずつ順に光らせる指定、`.reversing` は端まで行ったら折り返す指定です。この二つを外すと全レイヤーが同時に明滅し、せわしない印象になります。私は最初 `.cumulative`（積み上げ式）で作ってしまい、光が溜まっていく見た目がダウンロードの「減っていく」感覚と噛み合わず、作り直しました。エフェクトの選択は、見た目の好みではなく、伝えたい状態の方向と合わせるのが結局は近道でした。

## エフェクトの使い分けを一枚で

手を動かしていると、どの場面でどのエフェクトを選ぶかの判断がだんだん固まってきます。現時点での私の使い分けを表にまとめます。

<table>
<thead>
<tr><th>エフェクト</th><th>動き</th><th>向く場面</th><th>駆動方法</th></tr>
</thead>
<tbody>
<tr><td><strong>.bounce</strong></td><td>一度だけ跳ねる</td><td>お気に入り・いいね・送信完了</td><td>value:（回数）</td></tr>
<tr><td><strong>.pulse</strong></td><td>ゆっくり明滅</td><td>処理中・注意喚起</td><td>isActive:</td></tr>
<tr><td><strong>.variableColor</strong></td><td>レイヤー順に発光</td><td>電波・ダウンロード・段階進行</td><td>isActive:</td></tr>
<tr><td><strong>.scale</strong></td><td>拡大縮小</td><td>選択中の強調</td><td>isActive: / value:</td></tr>
<tr><td><strong>.replace</strong></td><td>別シンボルへ差し替え</td><td>再生↔停止・ミュート切替</td><td>contentTransition</td></tr>
</tbody>
</table>

`.replace` だけは少し毛色が違い、`symbolEffect` ではなく `contentTransition` として指定します。次の節で扱います。

## 状態が切り替わるアイコンはcontentTransitionで

再生ボタンと一時停止ボタンのように、タップでシンボル自体が入れ替わるUIは多くあります。単に `Image` を差し替えるとパッと切り替わるだけですが、`contentTransition(.symbolEffect(.replace))` を添えると、片方が縮んで消え、もう片方が膨らんで現れる補間が入ります。地味ですが、押した対象がそのまま変化した、という納得感が生まれます。

```swift
struct PlayPauseButton: View {
    @State private var isPlaying = false

    var body: some View {
        Button {
            isPlaying.toggle()
        } label: {
            Image(systemName: isPlaying ? "pause.fill" : "play.fill")
                .font(.system(size: 40))
                .contentTransition(.symbolEffect(.replace))
        }
    }
}
```

`.replace` には `.downUp` や `.offUp` といった方向のバリエーションもあります。ミュートのオン・オフのように意味に上下がある切り替えでは方向を指定すると、動きが状態の意味に沿って、より自然に感じられます。

## OSバージョンの出し分けを最初に決めておく

ここが実装前にいちばん詰まりやすい落とし穴です。`symbolEffect` は iOS 17 から、`.replace` の一部方向指定や新しいエフェクトは iOS 18 から、と導入時期が分かれています。Rork Max で生成したアプリでも、対応する最小 iOS を下げるほど、使えるエフェクトに制約が出ます。

私は「エフェクトは装飾であって、無くても機能は成立する」という原則で線を引いています。つまり、古い OS ではアニメーションを回避して静的アイコンにフォールバックし、新しい OS でだけ動かす。この割り切りにしておくと、`if #available` の分岐が最小で済みます。この方針を推奨します。

```swift
struct AdaptiveFavorite: View {
    let tapCount: Int
    let isFavorite: Bool

    var body: some View {
        let icon = Image(systemName: isFavorite ? "heart.fill" : "heart")
            .font(.system(size: 32))

        if #available(iOS 17.0, *) {
            icon.symbolEffect(.bounce, value: tapCount)
        } else {
            // iOS 16以前は静的アイコンで機能だけ担保する
            icon
        }
    }
}
```

補足しておくと、`if #available` はビュー修飾子のチェーンの途中では書けません。上のように、いったんベースの `Image` を定数に取り出してから分岐で修飾を分けると、コンパイラも読み手も混乱しません。この形に落ち着くまで、私は三通りほど遠回りをしました。

## 動かしすぎない、という一番大事な線引き

最後は失敗談です。楽しくなって、画面内のほぼ全部のアイコンに `pulse` を付けた版を一度作りました。結果は、どこを見ればいいのか分からない、落ち着かない画面でした。動きは注意を引く道具なので、常時動く要素が増えるほど、一つ一つの動きの意味が薄れます。

いま私が守っているのは、次の三つの目安です。

1. 常時アニメーションは同一画面に一つまでに絞る
2. ワンショットは操作への反応にだけ使う
3. `.pulse` のような明滅は待たせている最中にだけ出し、完了したら必ず止める

動きは足し算ではなく引き算で決める。派手なエフェクトを一つ入れるより、静けさの中で一箇所だけ反応する方が、指と画面の呼吸が合います。癒し系のアプリを作っていると、この静けさの設計こそが体験の芯だと感じます。

Rork Max が本物の Swift を出してくれるおかげで、こうした細かな手触りの調整まで iOS の標準機能に素直に乗せられます。小さな動き一つで、押した実感は驚くほど変わります。実装の参考になれば幸いです。お読みいただき、ありがとうございました。
