---
title: "Rork で地図ベースのスポット共有アプリを構築する — MapView × Supabase 実践チュートリアル"
slug: "rork-mapview-supabase-spot-sharing-app"
category: "rork-dev"
level: "intermediate"
premium: true
date: "2026-03-24T16:00"
author: "Rork Lab"
description: "Rork と React Native MapView、Supabase を組み合わせて、お気に入りスポットを地図上に共有できるアプリを構築する実践チュートリアル。位置情報・画像アップロード・リアルタイム同期まで完全解説。"
tags: ["Rork", "MapView", "Supabase", "地図アプリ", "位置情報", "プレミアム", "チュートリアル"]
---

## はじめに：地図アプリの需要と Rork の強み

「おすすめのカフェを友達と共有したい」「旅行先で見つけた絶景スポットを記録したい」——こうしたニーズに応える地図ベースのスポット共有アプリは、App Store・Google Play ともに根強い人気があります。

しかし、地図表示、位置情報取得、画像アップロード、データベース連携といった複数の機能を実装するのは、従来の開発手法では相当な時間を要しました。Rork を使えば、AI がこれらの複雑な実装を支援し、**アイデアから動くプロトタイプまでの時間を大幅に短縮**できます。

本記事では、Rork × React Native MapView × Supabase で以下の機能を持つスポット共有アプリを構築します：

- 地図上にスポットをピン表示
- 現在地の取得とスポットの新規登録
- 写真の撮影・アップロード
- 他のユーザーが登録したスポットのリアルタイム同期

## Supabase プロジェクトのセットアップ

### テーブル設計

Supabase のダッシュボードまたは SQL エディタで以下のテーブルを作成します：

```sql
-- スポットテーブル
CREATE TABLE spots (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) NOT NULL,
  title TEXT NOT NULL CHECK (char_length(title) <= 100),
  description TEXT CHECK (char_length(description) <= 500),
  latitude DOUBLE PRECISION NOT NULL,
  longitude DOUBLE PRECISION NOT NULL,
  image_url TEXT,
  category TEXT DEFAULT 'general',
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- RLS（行レベルセキュリティ）ポリシー
ALTER TABLE spots ENABLE ROW LEVEL SECURITY;

-- 全ユーザーが閲覧可能
CREATE POLICY "Anyone can view spots"
  ON spots FOR SELECT USING (true);

-- 認証済みユーザーのみ作成可能
CREATE POLICY "Authenticated users can create spots"
  ON spots FOR INSERT
  WITH CHECK (auth.uid() = user_id);

-- 自分のスポットのみ更新可能
CREATE POLICY "Users can update own spots"
  ON spots FOR UPDATE
  USING (auth.uid() = user_id);

-- 期待出力: テーブルとRLSポリシーが正常に作成される
```

### Supabase クライアントの初期化

```typescript
// src/lib/supabase.ts
import { createClient } from "@supabase/supabase-js";
import AsyncStorage from "@react-native-async-storage/async-storage";

const supabaseUrl = "https://your-project.supabase.co";
const supabaseAnonKey = "your-anon-key";

export const supabase = createClient(supabaseUrl, supabaseAnonKey, {
  auth: {
    storage: AsyncStorage,
    autoRefreshToken: true,
    persistSession: true,
    detectSessionInUrl: false,
  },
});
// 期待出力: Supabase クライアントが初期化され、AsyncStorage でセッションが永続化
```

## MapView でスポットを表示する

React Native Maps を使って、地図上にスポットをピン表示するコアコンポーネントを実装します。

### 地図コンポーネント

```tsx
// src/components/SpotMap.tsx
import React, { useState, useEffect, useCallback } from "react";
import { View, StyleSheet, Alert } from "react-native";
import MapView, { Marker, Callout, Region } from "react-native-maps";
import * as Location from "expo-location";
import { supabase } from "../lib/supabase";

interface Spot {
  id: string;
  title: string;
  description: string | null;
  latitude: number;
  longitude: number;
  image_url: string | null;
  category: string;
}

export function SpotMap() {
  const [spots, setSpots] = useState<Spot[]>([]);
  const [region, setRegion] = useState<Region>({
    latitude: 35.6812,  // 東京駅（デフォルト）
    longitude: 139.7671,
    latitudeDelta: 0.05,
    longitudeDelta: 0.05,
  });

  // 現在地を取得
  useEffect(() => {
    (async () => {
      const { status } = await Location.requestForegroundPermissionsAsync();
      if (status !== "granted") {
        Alert.alert("位置情報の許可が必要です");
        return;
      }
      const location = await Location.getCurrentPositionAsync({});
      setRegion((prev) => ({
        ...prev,
        latitude: location.coords.latitude,
        longitude: location.coords.longitude,
      }));
    })();
  }, []);

  // スポットをリアルタイムで取得
  useEffect(() => {
    const fetchSpots = async () => {
      const { data, error } = await supabase
        .from("spots")
        .select("*")
        .order("created_at", { ascending: false })
        .limit(200);

      if (!error && data) setSpots(data);
    };

    fetchSpots();

    // リアルタイムサブスクリプション
    const channel = supabase
      .channel("spots-changes")
      .on(
        "postgres_changes",
        { event: "INSERT", schema: "public", table: "spots" },
        (payload) => {
          setSpots((prev) => [payload.new as Spot, ...prev]);
        }
      )
      .subscribe();

    return () => {
      supabase.removeChannel(channel);
    };
  }, []);

  return (
    <View style={styles.container}>
      <MapView
        style={styles.map}
        region={region}
        onRegionChangeComplete={setRegion}
        showsUserLocation
        showsMyLocationButton
      >
        {spots.map((spot) => (
          <Marker
            key={spot.id}
            coordinate={{
              latitude: spot.latitude,
              longitude: spot.longitude,
            }}
            pinColor={getCategoryColor(spot.category)}
          >
            <Callout>
              <View style={styles.callout}>
                <Text style={styles.calloutTitle}>{spot.title}</Text>
                {spot.description && (
                  <Text style={styles.calloutDesc}>{spot.description}</Text>
                )}
              </View>
            </Callout>
          </Marker>
        ))}
      </MapView>
    </View>
  );
}

function getCategoryColor(category: string): string {
  const colors: Record<string, string> = {
    cafe: "#8B4513",
    restaurant: "#FF6347",
    nature: "#228B22",
    shopping: "#4169E1",
    general: "#FF4500",
  };
  return colors[category] || colors.general;
}

const styles = StyleSheet.create({
  container: { flex: 1 },
  map: { flex: 1 },
  callout: { padding: 8, maxWidth: 200 },
  calloutTitle: { fontWeight: "bold", fontSize: 14 },
  calloutDesc: { fontSize: 12, color: "#666", marginTop: 4 },
});
```

## スポット登録と画像アップロード

ユーザーが新しいスポットを登録する画面を実装します。カメラで写真を撮影し、Supabase Storage にアップロードする機能も含みます。

### スポット登録フォーム

```tsx
// src/components/AddSpotForm.tsx
import React, { useState } from "react";
import { View, TextInput, TouchableOpacity, Text, Image, Alert } from "react-native";
import * as ImagePicker from "expo-image-picker";
import * as Location from "expo-location";
import { supabase } from "../lib/supabase";
import { decode } from "base64-arraybuffer";

interface AddSpotFormProps {
  onSuccess: () => void;
}

export function AddSpotForm({ onSuccess }: AddSpotFormProps) {
  const [title, setTitle] = useState("");
  const [description, setDescription] = useState("");
  const [category, setCategory] = useState("general");
  const [imageUri, setImageUri] = useState<string | null>(null);
  const [loading, setLoading] = useState(false);

  const pickImage = async () => {
    const result = await ImagePicker.launchCameraAsync({
      mediaTypes: ImagePicker.MediaTypeOptions.Images,
      quality: 0.7,
      base64: true,
    });

    if (!result.canceled && result.assets[0]) {
      setImageUri(result.assets[0].uri);
    }
  };

  const uploadImage = async (base64: string): Promise<string | null> => {
    const fileName = `spots/${Date.now()}.jpg`;
    const { data, error } = await supabase.storage
      .from("spot-images")
      .upload(fileName, decode(base64), {
        contentType: "image/jpeg",
      });

    if (error) {
      console.error("画像アップロード失敗:", error);
      return null;
    }

    const { data: urlData } = supabase.storage
      .from("spot-images")
      .getPublicUrl(data.path);

    return urlData.publicUrl;
  };

  const handleSubmit = async () => {
    if (!title.trim()) {
      Alert.alert("タイトルを入力してください");
      return;
    }

    setLoading(true);
    try {
      // 現在地を取得
      const location = await Location.getCurrentPositionAsync({});
      const { latitude, longitude } = location.coords;

      // 画像があればアップロード
      let imageUrl: string | null = null;
      if (imageUri) {
        const response = await fetch(imageUri);
        const blob = await response.blob();
        const reader = new FileReader();
        const base64 = await new Promise<string>((resolve) => {
          reader.onloadend = () => {
            const result = reader.result as string;
            resolve(result.split(",")[1]);
          };
          reader.readAsDataURL(blob);
        });
        imageUrl = await uploadImage(base64);
      }

      // Supabase にスポットを登録
      const { data: userData } = await supabase.auth.getUser();
      const { error } = await supabase.from("spots").insert({
        user_id: userData.user?.id,
        title: title.trim(),
        description: description.trim() || null,
        latitude,
        longitude,
        image_url: imageUrl,
        category,
      });

      if (error) throw error;

      Alert.alert("スポットを登録しました！");
      onSuccess();
    } catch (error) {
      Alert.alert("登録に失敗しました", (error as Error).message);
    } finally {
      setLoading(false);
    }
  };

  return (
    <View style={{ padding: 16 }}>
      <TextInput
        placeholder="スポット名"
        value={title}
        onChangeText={setTitle}
        style={{ borderWidth: 1, borderColor: "#ddd", padding: 12, borderRadius: 8, marginBottom: 12 }}
      />
      <TextInput
        placeholder="説明（任意）"
        value={description}
        onChangeText={setDescription}
        multiline
        style={{ borderWidth: 1, borderColor: "#ddd", padding: 12, borderRadius: 8, marginBottom: 12, height: 80 }}
      />
      <TouchableOpacity onPress={pickImage} style={{ backgroundColor: "#4A90D9", padding: 12, borderRadius: 8, marginBottom: 12 }}>
        <Text style={{ color: "#fff", textAlign: "center" }}>写真を撮影</Text>
      </TouchableOpacity>
      {imageUri && <Image source={{ uri: imageUri }} style={{ width: "100%", height: 200, borderRadius: 8, marginBottom: 12 }} />}
      <TouchableOpacity onPress={handleSubmit} disabled={loading} style={{ backgroundColor: loading ? "#ccc" : "#FF4500", padding: 14, borderRadius: 8 }}>
        <Text style={{ color: "#fff", textAlign: "center", fontWeight: "bold" }}>
          {loading ? "登録中..." : "スポットを登録"}
        </Text>
      </TouchableOpacity>
    </View>
  );
}
// 期待出力: スポットが正常に登録され、地図にリアルタイムで反映
```

## カテゴリフィルターとスポット検索

ユーザーがカテゴリ別にスポットを絞り込めるフィルター機能を追加します。

```typescript
// src/hooks/useSpotFilter.ts
import { useState, useMemo } from "react";

interface Spot {
  id: string;
  title: string;
  category: string;
  latitude: number;
  longitude: number;
}

const CATEGORIES = [
  { id: "all", label: "すべて" },
  { id: "cafe", label: "カフェ" },
  { id: "restaurant", label: "レストラン" },
  { id: "nature", label: "自然" },
  { id: "shopping", label: "ショッピング" },
  { id: "general", label: "その他" },
] as const;

export function useSpotFilter(spots: Spot[]) {
  const [selectedCategory, setSelectedCategory] = useState("all");
  const [searchQuery, setSearchQuery] = useState("");

  const filteredSpots = useMemo(() => {
    return spots.filter((spot) => {
      const matchesCategory =
        selectedCategory === "all" || spot.category === selectedCategory;
      const matchesSearch =
        !searchQuery ||
        spot.title.toLowerCase().includes(searchQuery.toLowerCase());
      return matchesCategory && matchesSearch;
    });
  }, [spots, selectedCategory, searchQuery]);

  return {
    categories: CATEGORIES,
    selectedCategory,
    setSelectedCategory,
    searchQuery,
    setSearchQuery,
    filteredSpots,
  };
}
// 期待出力: カテゴリと検索クエリでフィルタリングされたスポット配列
```

## よくある質問（FAQ）

### Q: 地図表示に Google Maps と Apple Maps のどちらが使われますか？

A: React Native Maps はプラットフォームに応じて自動的に使い分けます。iOS では Apple Maps がデフォルトですが、`provider="google"` を指定すれば Google Maps も利用可能です。Android では Google Maps がデフォルトです。Rork でビルドする場合、Expo の設定で Google Maps API キーを追加するだけで対応できます。

### Q: Supabase の無料枠でどのくらいのスポットデータを保存できますか？

A: Supabase の無料プランでは、データベース500MB、ストレージ1GBまで利用可能です。1スポットあたり約1KBのデータ量なので、テキストデータだけなら50万件以上保存できます。画像を含める場合は、1枚あたり100KB程度に圧縮すれば約10,000枚保存可能です。

### Q: リアルタイム同期のパフォーマンスは大丈夫ですか？

A: Supabase の Realtime 機能は WebSocket ベースで、数百人程度の同時接続であれば問題なく動作します。表示するスポット数は `limit(200)` で制限しているため、地図のレンダリングパフォーマンスにも影響しません。大規模なアプリでは、表示範囲内のスポットのみを取得する地理的クエリの実装を検討してください。

## まとめ：Rork で地図アプリ開発を始めよう

本記事では、Rork × React Native MapView × Supabase で地図ベースのスポット共有アプリを構築する方法を解説しました。

- **Supabase セットアップ**: テーブル設計とRLSセキュリティポリシー
- **MapView 連携**: スポットのピン表示とリアルタイム更新
- **画像アップロード**: カメラ撮影 → Supabase Storage への保存
- **フィルター機能**: カテゴリ別絞り込みとテキスト検索

Rork の AI 支援と Supabase のバックエンド機能を組み合わせれば、複雑な地図アプリでも効率的にプロトタイプを構築できます。地図と位置情報の基本については「[Rork Max Maps & Location ガイド](/articles/rork-dev/rork-max-maps-location)」を、Supabase との認証連携については「[Rork 認証ガイド — Firebase & Supabase](/articles/rork-dev/rork-auth-firebase-supabase)」もあわせてご覧ください。
